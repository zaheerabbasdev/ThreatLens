# Copy to .env.local for local development. Never commit the real .env.local file.

# Leave unset to run entirely on mock services (default, no backend needed —
# same as every phase before Phase 12). Set to the running backend's
# versioned API root to use the real Api*Service implementations instead
# (see src/services/index.ts, src/services/api/).
# VITE_API_BASE_URL=http://localhost:4000/api/v1
